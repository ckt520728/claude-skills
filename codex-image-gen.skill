---
name: codex-image-gen
description: "由 Claude 生成優化的圖像設計 prompt，用戶再自行貼到 Codex 環境生圖。觸發詞：「幫我生圖」「畫一張」「設計圖案」「生成圖片」「image gen」「codex 生圖」「用 AI 畫」「生成封面」「製作縮圖」「banner 設計」「logo 設計」「infographic 圖片」「幫我寫 prompt」「生圖 prompt」。不適用：需要互動式向量設計（用 Canva MCP）、文件排版（用 pptx/pdf skill）。"
---

# Codex Image Gen Skill

Claude 根據用戶描述生成專業 image prompt，用戶再貼到 Codex 環境生圖。

---

## Core Workflow

### Phase 1 — 需求訪談（最多問 3 項缺少的資訊，已知的不問）

| 要素 | 範例 |
|------|------|
| **主題 / 用途** | YouTube 封面、教學海報、社群貼文、Logo |
| **風格** | 科技感、手繪、極簡、賽博龐克、水彩、3D 渲染 |
| **色調** | 深藍+金色、莫蘭迪色、高對比黑白 |
| **尺寸 / 比例** | 1:1（方形）、16:9（橫）、9:16（直）、自由 |
| **文字需求** | 是否要在圖中加標題文字（Image Gen 文字效果有限，建議後製）|

### Phase 2 — 生成 Image Prompt

生成**英文** prompt（Codex Image Gen 對英文效果最好）。

**Prompt 結構：**

```
[SUBJECT]: 主體描述，具體清晰
[STYLE]: 藝術風格 + 技法（例如 digital art, cinematic lighting, 8K）
[COMPOSITION]: 構圖（例如 rule of thirds, subject centered）
[COLOR]: 色調（例如 deep blue and gold palette, high contrast）
[MOOD]: 氛圍（例如 futuristic, professional, inspiring）
[TECHNICAL]: sharp focus, high detail, no artifacts, no distortion
[NEGATIVE]: no text, no watermark（如用戶不要文字入圖）
```

**輸出格式：**

向用戶展示完整 prompt，格式如下：

```
📋 Codex Image Prompt（直接複製貼到 Codex）

──────────────────────────────
{完整英文 prompt}
──────────────────────────────

📐 建議尺寸：{1024×1024 / 1536×1024 / 1024×1536}
🎨 建議品質：low（草稿）→ 確認構圖後再用 high（最終版）
```

詢問用戶是否需要調整 prompt，確認後即完成。

### Phase 3 — 迭代（選用）

若用戶貼回 Codex 生出的圖後想調整：
- 描述哪裡不滿意（色調？構圖？風格？）
- Claude 修改 prompt 並重新輸出，用戶再貼回 Codex

---

## Prompt 工程速查表

### 風格關鍵詞

| 風格 | 英文關鍵詞 |
|------|-----------|
| 科技感 | `futuristic, holographic, neon glow, cyberpunk` |
| 極簡 | `minimalist, clean lines, white space, flat design` |
| 專業商務 | `corporate, professional, sophisticated, sleek` |
| 手繪插畫 | `hand-drawn illustration, sketch style, ink lines` |
| 3D 渲染 | `3D render, octane render, volumetric lighting` |
| 水彩 | `watercolor painting, soft edges, paper texture` |
| 電影感 | `cinematic, dramatic lighting, film grain` |

### 光線關鍵詞

| 效果 | 英文關鍵詞 |
|------|-----------|
| 柔和環境光 | `soft ambient light, diffused lighting` |
| 戲劇性 | `dramatic rim light, strong shadows` |
| 黃金時刻 | `golden hour, warm sunset tones` |
| 工作室感 | `studio lighting, white background, product shot` |

### 品質固定加入

```
sharp focus, high detail, professional quality, no artifacts, no distortion
```

---

## 常見問題排除

| 問題 | Prompt 調整方式 |
|------|----------------|
| 圖中文字扭曲 | 加 `no text, text-free`，文字改用 Canva 後製 |
| 臉孔變形 | 加 `photorealistic face, symmetrical features, correct anatomy` |
| 背景雜亂 | 加 `clean background, minimal background, solid color background` |
| 色調不準 | 改用十六進位色碼：`deep navy blue #1a237e, golden yellow #ffd700` |
| 效果不如預期 | 先用 low 品質確認構圖，滿意後再切 high |
