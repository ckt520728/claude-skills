# 專案收尾 — 踩過的坑與可重用做法

**專案：** 約束式（banded）Holo-Hilbert Spectral Analysis — 把生理頻帶當硬約束，跨 Python / MATLAB 兩套實作驗證，並產出一份「怎麼讀這些圖」的教學文件
**日期：** 2026-09-04（單一工作階段）
**成果：** 兩套保留時間軸的 banded HHSA 引擎（Python + MATLAB/Octave）、九組參數設定的交叉比較、六張圖、一份 K-10 閱讀層級的圖表判讀指南（markdown + 可發布網頁）
**環境：** Windows 10、PowerShell 5.1 / Git Bash、Python 3.14、GNU Octave 11.3、Claude Code

延續 `2026-08-20_CGMS_HHSA_Case_Lessons_and_Pitfalls.md` 與 `2026-08-21_HHSA_Python_vs_MATLAB_Lessons_and_Pitfalls.md`。本文只寫**這次新踩到的坑**，前兩份已寫過的（PDF 還原、PCHIP 正規化、cp950 console）不重複。

> **去識別化與保密聲明：** 本 repo 為公開。
> 本次分析所用的臨床紀錄已移除姓名、裝置序號、報告編號與確切日期，原始 PDF 與還原後的 CSV **一律不進 repo**，本文亦不附任何以該紀錄繪製的圖。
> 此外，本次工作有一部分屬於**同儕審查**，依審查保密義務，稿件識別資訊、標題、作者、以及任何針對該稿件的具體意見**一律不出現在本文**。以下只保留與稿件無關、可重用的方法學與工具鏈教訓。

---

## 零、一句話總結

這次真正學到的東西可以壓成三句：

| # | 教訓 | 適用範圍 |
|---|---|---|
| 1 | **任何「約束式表徵」都必須報告保留比例。** 把頻帶同時套在載波軸與調幅軸上，實測只有 20–29 % 的第二層振幅活得下來 | 所有 binning / gridding 的特徵工程 |
| 2 | **兩套實作看起來不一樣時，先把規格對齊到最後一個細節，再談誰對誰錯。** 對齊後 6/6 模態 \|r\| ≥ 0.96；沒對齊時同一筆資料的 2D 譜餘弦相似度只有 0.547 | 任何跨語言 / 跨團隊的重現 |
| 3 | **「畫出圖」和「別人讀得懂圖」是兩件不同的交付物。** 六面板熱圖在一般欄寬下根本無法判讀 | 所有要交出去的科學圖表 |

---

## 一、頻帶約束的坑（這節最貴）

### 1. 約束「調幅軸 ω」和約束「載波軸 f」完全是兩回事

**症狀：** 把五個生理頻帶當硬約束同時套在 *f* 與 *ω* 上，跑完發現大部分能量憑空消失。

**實測：** 九組參數設定裡，通過約束進入 (f, ω) 網格的第二層振幅只有 **20–29 %**。最大宗的損失在**調幅軸**（43–66 %），而不是載波軸。

**原因：** 一段 13 天紀錄裡，某個模態的**包絡本身**多半比 24 小時還慢——「日間擺盪幅度的跨日漂移」是很自然的東西，但如果頻帶集合最慢只到 24 小時，就沒有任何一格容得下它。

**要點：** 把載波約束到生理頻帶是標準作法，不需要辯護；把**調幅**約束到同一組頻帶，等於斷言「沒有任何節律是在長於一天的尺度上被調幅的」——這是一個很強的生理假設，必須明講並檢定。**最小修正是：只約束載波軸，讓 ω 自由。**

### 2. 頻帶邊界不要放在訊號自己的週期上

**症狀：** 「晝夜節律」頻帶定義成 24 h – 8 h，結果日節律模態常常被判到**所有頻帶之外**。

**實測：** 該模態的平均瞬時週期是 **24.04–24.18 小時**，比邊界只慢不到 1 %。八種分解裡有**六種**把它判為 OUTSIDE。

**要點：** 23.9 h 和 24.1 h 是同一件生理，卻落在不同頻帶、或不落在任何頻帶。邊界要離眾數週期一個安全距離（改成 26–28 h 不花任何代價），或改用適應性定義（「平均週期最接近 24 h 的那個模態」）。**任何把邊界壓在資料眾數上的分箱，都是在讓分解方式代替生理學做決定。**

### 3. PyEMD 的 `noise_width` 是乘「全距」，不是乘 SD

**症狀：** 論文/程式碼寫「使用 PyEMD 預設值」，看起來很無害。

**實測：** PyEMD 把注入雜訊的標準差設為 `noise_width × (max − min)`，`noise_width = 0.05`。對一筆全距 15.6 的訊號，這是 0.78，也就是 **0.263 × SD**——明顯高於 EEMD 文獻慣用的 0.2 × SD。

**後果不是小數點：** 同一筆資料，這個雜訊水準下 EEMD 產生 10 個模態（其中兩個數值上是空的），普通 EMD 只產生 6 個；而且它直接決定日節律模態落在哪個頻帶。

**要點：** 「用預設值」不是一個可重現的陳述。報告雜訊時要**同時給絕對值與 SD 倍數**，並註明該套件是以什麼為尺度。

### 4. 「指派」這個動作要寫清楚粒度

**症狀：** 同一份規格，「依平均瞬時頻率把每個模態指派到一個頻帶」（逐模態）和「每個時間點各自指派」（逐點）都說得通。

**實測：** 同一次執行，晝夜節律載波列在兩種讀法下是 **8.61 %** 對 **29.85 %**——差 3.5 倍。

**要點：** pipeline 裡只要出現「指派 / 歸類 / 分箱」，就要寫明是對整個模態做、還是逐樣本做。這種歧義不會報錯，只會安靜地給你兩張不同的表。

### 5. 先強制 ω < f，再看「最大格」

**症狀：** 共調變圖的最大格落在「載波與調幅同一頻帶」的格子上，看起來像重大發現。

**要點：** 兩軸同頻帶時 ω ≈ f，包絡的變化速度和它要調變的東西差不多快——這是**退化**，不是振幅調變。實測強制 ω < f 只花掉 6.2 % 的入格振幅，很便宜。**先遮罩再解讀，不要反過來。**

---

## 二、跨實作比較的坑

### 6. 對齊規格之前，不要下「哪一邊寫錯了」的結論

**實測：** 同一筆資料、同一組頻帶：

| 比較 | C(f, ω) 餘弦相似度 |
|---|---|
| 規格完全對齊（同 EMD、同包絡、同篩選樣條） | 0.902；6/6 模態 \|r\| ≥ 0.96，週期最大差 2.98 % |
| 兩邊各用自己的參考參數 | **0.547** |

**要點：** 0.547 看起來像有人寫錯了，其實兩邊都沒錯——差異全部來自**方法選擇**（EEMD vs masking EMD、解析包絡 vs 樣條包絡）。跨實作比較的第一步永遠是「把規格對齊到最後一個細節」，對齊後還有差異才值得抓 bug。

### 7. 彙整會藏住分歧——要同時報告彙整前後

**實測：** 2D 譜的餘弦相似度 0.547，彙整成 5×5 頻帶表後升到 **0.818**。

**要點：** 彙整（pooling）確實買到穩健性，這是好消息、值得報告；但**只報告彙整後的穩定度會嚴重高估一致性**。兩個數字要一起出現。另外，即使彙整後相似度 0.818，「最大的那一格」仍然會隨參數移動——所以「最大格是哪一格」這種讀法本身就不穩健。

### 8. MATLAB 授權掛掉時，Octave 可以代跑，但 provenance 要寫清楚

**做法：** 每個 `.m` 都寫成不依賴任何 toolbox 的純 MATLAB 語法（例如自己算 analytic signal，不要呼叫 Signal Processing Toolbox 的 `hilbert()`），這樣同一批檔案在 Octave 與 MATLAB 下都能跑。

**要點：** 必須明講「這是依規格重新實作，不是原始程式碼的 byte-level 重現」。用 Octave 代跑是可接受的工程折衷，把它說成「重現了對方的實作」則不是。

---

## 三、環境的坑

### 9. Bash heredoc 在這個 harness 裡會炸

**症狀：** 用 `cat > file.md <<'EOF' … EOF` 寫長檔案，回 `unexpected EOF while looking for matching '`，即使用的是 quoted heredoc、內文只有普通的英文撇號。

**解法：** 長檔案一律用 Write 工具寫，不要用 heredoc。需要程式化改檔時，寫一支 `.py` 再執行，也不要把 Python 塞進 `python -c` 的引號地獄裡。

### 10. 驗證中文寫檔，要用 `file` / `grep`，不要用 `python -c print`

**症狀：** console 是 cp950，`python -c "print(中文)"` 會噴亂碼，讓人誤以為檔案壞了。

**解法：**

```bash
file out.md                 # 應該回 UTF-8 text
grep -n "關鍵字" out.md      # grep 的輸出是好的
```

**要點：** 檔案本身是有效 UTF-8，壞掉的只有終端機。**用讀檔的方式驗證寫檔，不要用 print。**

### 11. `grep -c` 找不到東西時 exit 1，會把 `&&` 鏈打斷

**症狀：** 驗證腳本寫成 `cmd1 && grep -c "壞東西" f && cmd2`，結果「沒有壞東西」（理想狀況）反而讓後面的 `cmd2` 不執行。

**解法：** 驗證鏈用 `;` 串，不要用 `&&`；或 `grep -c … || true`。

---

## 四、把成果發布成網頁的坑

### 12. 外部圖片會被 CSP 擋掉，而且不是每個帳號都有 `assets` 能力

**症狀：** 網頁裡 `<img src="https://…">` 靜默失效，什麼錯誤都看不到。

**解法：** 圖片改成 base64 data URI 內嵌。實測 1.89 MB 的 PNG 變成 2.5 MB 的頁面（上限 16 MB，還很寬裕）。

**要點：** 先確認可用的 runtime capability 清單再決定策略；沒有資產上傳能力時，內嵌是唯一路。

### 13. 內嵌圖片一定要用「模板 + 建置腳本」，不要手改產生出來的檔

因為 base64 會把原始碼撐爆到無法編輯，所以拆成三個檔：

| 檔案 | 角色 |
|---|---|
| `guide_template.html` | **可編輯的原始碼**，圖片位置寫 `{{FIG:name}}` 佔位符 |
| `build_guide.py` | 把 PNG 讀進來轉 base64、替換佔位符、檢查大小上限 |
| `guide.html` | **產生物，絕不手改** |

```python
PLACEHOLDER = re.compile(r"\{\{FIG:([A-Za-z0-9_]+)\}\}")

html = io.open(TEMPLATE, encoding="utf-8").read()
names = PLACEHOLDER.findall(html)
if not names:
    sys.exit("no {{FIG:...}} placeholders found")

cache = {}
for name in names:
    if name in cache:
        continue
    raw = open(os.path.join(FIGDIR, name + ".png"), "rb").read()
    cache[name] = "data:image/png;base64," + base64.b64encode(raw).decode("ascii")

html = PLACEHOLDER.sub(lambda m: cache[m.group(1)], html)
io.open(TARGET, "w", encoding="utf-8").write(html)

if os.path.getsize(TARGET) > 16 * 1048576:
    sys.exit("ERROR: over the 16 MB limit")
```

### 14. 重新發布要用**同一個檔案路徑**

換路徑 = 產生一個新的頁面、新的網址。跨對話更新時要顯式帶上原網址，否則會多出一份孤兒。**把網址記進 handoff。**

---

## 五、文件維護的坑

### 15. 翻譯要「打補丁」，不要重譯

**症狀：** 英文版從 v2 升到 v3，中文版落後。直覺是整份重譯——但那會把既有 84 KB 的好翻譯全部丟掉重來，而且新舊版無法對照。

**解法：** 先 diff 出實際變動（本次是 13 個 hunk / 120 行），只翻譯 delta，然後用腳本套用，**且每個替換都斷言恰好命中一次**：

```python
def apply(path_in, path_out, edits, label):
    text = io.open(path_in, encoding="utf-8").read()
    for i, (old, new) in enumerate(edits, 1):
        n = text.count(old)
        if n != 1:
            sys.exit("%s edit %d matched %d times (need exactly 1)" % (label, i, n))
        text = text.replace(old, new)
    io.open(path_out, "w", encoding="utf-8").write(text)
```

**要點：** `n != 1` 就中止，是這個做法的全部價值——它讓「錨點文字漂移了」變成一個大聲的失敗，而不是一次安靜的漏改。翻譯慣例（哪些詞保留原文、哪些術語固定對應）要寫在譯文檔頭的說明區塊裡，下次補丁才對得起來。

### 16. 一直往後追加 session log，會讓前面的編號章節過期

**症狀：** handoff.md 前半是「第 1–7 節：現況」，後半是一段段日期追加的 session log。追加三次之後，第 1–7 節描述的是三個月前的狀態，但讀者會把它當現況看。

**解法：** 定期重構成兩層——**編號章節 = 現況，日期 log = 歷史**，並在檔頭寫明這個分工。重構前先存一份 `handoff_<date>_pre-restructure.md`。

**要點：** 這種文件的失敗模式不是「寫得不夠」，是「讀者不知道哪一段還算數」。

---

## 六、可重用產出

### 「怎麼讀這張圖」本身就是一份交付物

這次最有用的產出不是圖，是**教人讀圖的文件**。結構如下，可以直接搬去用在任何密集的科學圖表上：

1. **一分鐘版**：用生活類比講清楚兩個核心變數（本例：載波 = 電台頻率，調幅 = 音量起伏）
2. **詞彙表**：每個術語第一次出現就定義；雙語對照
3. **單位換算表**：頻率 ↔ 週期，把讀者最常算錯的那步先算好
4. **版面地圖**：面板配置圖，學一次就能套用到所有同版面的圖
5. **逐面板判讀**：每個面板「先看哪裡、亮點代表什麼、暗區代表什麼、陷阱在哪」
6. **陷阱清單**：飽和的白色不是數字、每個面板有各自的色標、軸是對數的
7. **報告模板**：填空式的句型，強迫把「保留比例」和「參數依賴性」寫進每張圖說
8. **交付前檢查清單**

### 圖表本身的三個硬規定

- **時間軸平滑只能用於顯示**，且要在圖說裡明講，並保證沒有任何報告數字來自平滑後的陣列
- **邊界要裁掉**（本例每端 24 小時），且要說裁了多少
- **密集熱圖一定要能放大**。六面板熱圖在欄寬下完全無法判讀；沒有 click-to-enlarge 等於沒交付

---

## 七、下次要先問的問題

1. **這個約束會丟掉多少東西？** 在做任何 binning / gridding 之前先算保留比例。如果只剩 20 %，後面所有特徵的解釋範圍都要跟著縮。
2. **邊界壓在資料的眾數上了嗎？** 畫一張「模態週期 vs 頻帶邊界」的圖，一眼就看得出來。
3. **有第二套實作可以對照嗎？** 沒有的話，至少換一組參數重跑，把兩張表一起報。單一參數組的結果不該被呈現得像唯一可得的那一張。
4. **交出去的圖，收圖的人讀得懂嗎？** 如果需要口頭解說才能讀，那份解說就是缺少的交付物。
